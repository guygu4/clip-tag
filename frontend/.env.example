# API base URL. Leave empty or set to /api when frontend is served from same host as API.
# VITE_API_URL=

# Video: use a direct URL, OR use the backend proxy for Google Drive:
#   Set to /api/video and set VIDEO_SOURCE_URL in the backend to your Drive share link.
VITE_VIDEO_URL=/api/video
