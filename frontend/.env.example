# API base URL. Leave empty or set to /api when frontend is served from same host as API.
# For separate frontend host, set to full API URL (e.g. https://yourapp.com/api).
# VITE_API_URL=

# Video URL (use an online link; local videos in public/ are gitignored).
# Example – replace with your own video URL:
VITE_VIDEO_URL=https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/BigBuckBunny.mp4
