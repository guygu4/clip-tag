{
  "name": "clip-tag-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "build": "echo 'No build step'",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "prisma": "^5.22.0"
  }
}
