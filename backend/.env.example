# PostgreSQL (required). For local dev use Neon free tier or Docker.
# Example (Neon / Render / etc.):
# DATABASE_URL="postgresql://user:password@host:5432/cliptag?sslmode=require"

# Allowed frontend origin for CORS (e.g. https://yourapp.onrender.com or http://localhost:5173)
FRONTEND_ORIGIN="http://localhost:5173"

PORT=3001

# Optional: path to frontend/dist when serving SPA from same server (e.g. ../frontend/dist when cwd is backend)
# FRONTEND_DIST=

# Video source URL for the /api/video proxy. Must be a direct video file URL:
# - Bunny.net: use the .mp4 URL (e.g. .../play_720p.mp4), not the HLS or dashboard page. Enable MP4 Fallback in library Encoding.
# - Google Drive: use the share link (we convert it).
# VIDEO_SOURCE_URL=
# Optional: for Bunny.net referrer restriction, set to your app URL (e.g. https://yourapp.onrender.com or http://localhost:5173).
# VIDEO_SOURCE_REFERER=
