# PostgreSQL (required). For local dev use Neon free tier or Docker.
# Example (Neon / Render / etc.):
# DATABASE_URL="postgresql://user:password@host:5432/cliptag?sslmode=require"

# Allowed frontend origin for CORS (e.g. https://yourapp.onrender.com or http://localhost:5173)
FRONTEND_ORIGIN="http://localhost:5173"

PORT=3001

# Optional: path to frontend/dist when serving SPA from same server (e.g. ../frontend/dist when cwd is backend)
# FRONTEND_DIST=
